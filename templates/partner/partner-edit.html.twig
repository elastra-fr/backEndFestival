{% extends 'home/index.html.twig' %}

{% block title %}Edition | Partenaire{% endblock %}

{% block content %}
    

    {{ form_start(form, {'attr':{'class':'formBase flexCont'} }) }}

    <div class="formContainerBase flexCont">
    <h3>Modifier partenaire</h3>

    <div class="formGroup flexCont">
    {{ form_label(form.partnerName, 'Nom*') }}
        {{ form_widget(form.partnerName) }}
    </div>

    <div class="formGroup flexCont">
    {{ form_label(form.partnerLogo, 'Logo') }}
 {{ form_widget(form.file, {'attr': { 'class': 'form-control-file'}}) }}
    </div>

       <div id="imagePreview">
       
       
        {% if urlLogo %}
            <img src="{{ asset(urlLogo) }}" alt="Image" class="imagePreview" />
                <button type="button" onclick="removeImage()">Supprimer</button>
        {% endif %}
       
       </div>

    <div class="formGroup flexCont">
    {{ form_label(form.partnerDescription, 'Description') }}
{{ form_widget(form.partnerDescription) }}
    </div>

    <div class="formGroup flexCont">
    {{ form_label(form.partnerCategory, 'Catégorie*') }} 
    {{ form_widget(form.partnerCategory) }}
    </div>
        
    <button type="submit" class="button">Enregistrer Modifications</button>
    <a href="{{ path('app_admin_partner') }}" class="button">Annuler</a>

    </div>

    {{ form_end(form) }}
{% endblock %}
