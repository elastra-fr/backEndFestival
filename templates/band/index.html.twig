{% extends 'home/index.html.twig' %}

{% block title %}Style de musique{% endblock %}

{% block content %}
<div class="contentContainer">

  {% for label, messages in app.flashes %}
        <div class="flash-{{ label }}">
            {% for message in messages %}
                <p>{{ message }}</p>
            {% endfor %}
        </div>
    {% endfor %}


<h2>Gestion des Groupes</h2>
    <a href="{{ path('app_admin_band_new') }}" class="button">Créer groupe</a>

<h3>Liste des Groupes</h3>

 
   <ul class="listBase">
        {% for band in bands %}
            
            <li class="listBaseItem"><div class="listItemTitle"><img src="{{ asset('images/icons/guitar-solid.svg') }}" alt="Icon" class="listItemIcon" /><span>{{ band.name }} </span></div><div class="listItemAction"><a href="{{ path('app_admin_band_edit', {'id': band.id}) }}" class="button">Editer</a><a href="{{ path('app_admin_band_delete', {'id': band.id}) }}" class="button">Effacer</a></div></li>
        {% endfor %}
    </ul>

</div>
{% endblock %}