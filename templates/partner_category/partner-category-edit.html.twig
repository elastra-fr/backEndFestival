{# templates/partner_category/form.html.twig #}

{% extends 'home/index.html.twig' %}

{% block title %}Ajout | Edition categorie partenaire{% endblock %}

{% block content %}
    <div class="formBase flexCont">
        {{ form_start(form, {'attr': {'class': 'formBase flexCont'}}) }}

        <div class="formContainerBase flexCont">
            <h3>Editer une catégorie</h3>

            <div class="formGroup flexCont">
                {{ form_label(form.partnerCategory, 'Nom de la catégorie', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.partnerCategory, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.partnerCategory) }}
            </div>

            <button type="submit" class="button">Enregistrer modification</button>
            <a href="{{ path('app_admin_partner_category') }}" class="button">Annuler</a>
        </div>

        {{ form_end(form) }}
    </div>
{% endblock %}
