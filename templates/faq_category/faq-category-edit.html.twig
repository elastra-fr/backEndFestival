{% extends 'home/index.html.twig' %}

{% block title %}Edition | Section FAQ{% endblock %}

{% block content %}
    {{ form_start(form, {'attr' : {'class' : 'formBase flexCont'}}) }}
    <div class="formContainerBase flexCont">
    <h3>Editer une Section</h3>
    <div class="formGroup flexCont">
        {{ form_label(form.category, 'Section') }}
        {{ form_widget(form.category, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.category) }}
    </div>
    <button type="submit" class="button">Enregistrer modification</button>

    <a href="{{ path('app_admin_faq_category') }}" class="button">Annuler</a>
    {{ form_end(form) }}

    </div>
{% endblock %}
